<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.iotech.restaurant">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Login"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".Home"
            android:configChanges="screenSize|orientation"/>

        <activity android:name=".Cart"
            android:configChanges="screenSize|orientation"/>

        <activity android:name=".Invoice"
            android:configChanges="screenSize|orientation"/>

        <activity android:name=".Setting"
            android:configChanges="screenSize|orientation"/>

        <activity android:name=".ChangePassword" />

        <activity android:name=".ConfigIPServer" />

        <activity android:name=".SettingTable"
            android:configChanges="screenSize|orientation"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".DetailItemRestaurant"
            android:configChanges="screenSize|orientation"></activity>
    </application>

</manifest>